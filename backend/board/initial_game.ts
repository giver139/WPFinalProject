export function getFirstMove(): number {
  return 0;
}

export function getInitialBoard(): {initialBoard: number[], board: number[]} {
  return {initialBoard: new Array(32).fill(0), board: new Array(32).fill(0)};
}
